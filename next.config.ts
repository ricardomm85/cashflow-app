import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

// Note: if Turbopack panics persist, run dev with: next dev --no-turbopack
export default nextConfig;
